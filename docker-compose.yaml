version: '3.8'

services:
  gaz-eye:
    build: .
    container_name: gaz_eye_container
    restart: always
    volumes:
      # Optional: bind-mount stations.yaml to allow updates without rebuild
      - ./stations.yaml:/app/stations.yaml
    environment:
      - TZ=America/Montreal
